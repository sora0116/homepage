---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "all-dividers", height, title } = Astro.props;
---

<DadsExampleFrame
  component="divider"
  example={example}
  height={height}
  title={title}
/>
