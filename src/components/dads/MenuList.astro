---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "has-children", height, title } = Astro.props;
---

<DadsExampleFrame
  component="menu-list"
  example={example}
  height={height}
  title={title}
/>
