---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "plain", height, title } = Astro.props;
---

<DadsExampleFrame
  component="breadcrumb"
  example={example}
  height={height}
  title={title}
/>
