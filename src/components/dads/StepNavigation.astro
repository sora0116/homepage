---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "playground-full", height, title } = Astro.props;
---

<DadsExampleFrame
  component="step-navigation"
  example={example}
  height={height}
  title={title}
/>
