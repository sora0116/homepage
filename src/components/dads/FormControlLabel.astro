---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "multiple", height, title } = Astro.props;
---

<DadsExampleFrame
  component="form-control-label"
  example={example}
  height={height}
  title={title}
/>
