---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "example", height, title } = Astro.props;
---

<DadsExampleFrame
  component="tab"
  example={example}
  height={height}
  title={title}
/>
