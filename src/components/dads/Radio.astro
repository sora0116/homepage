---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "all-radios", height, title } = Astro.props;
---

<DadsExampleFrame
  component="radio"
  example={example}
  height={height}
  title={title}
/>
