---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "playground-consolidated", height, title } = Astro.props;
---

<DadsExampleFrame
  component="date-picker"
  example={example}
  height={height}
  title={title}
/>
