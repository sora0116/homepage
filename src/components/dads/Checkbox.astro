---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "all-checkboxes", height, title } = Astro.props;
---

<DadsExampleFrame
  component="checkbox"
  example={example}
  height={height}
  title={title}
/>
