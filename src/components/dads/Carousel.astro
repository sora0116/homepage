---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "container", height, title } = Astro.props;
---

<DadsExampleFrame
  component="carousel"
  example={example}
  height={height}
  title={title}
/>
