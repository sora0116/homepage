---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "all-lists", height, title } = Astro.props;
---

<DadsExampleFrame
  component="list"
  example={example}
  height={height}
  title={title}
/>
