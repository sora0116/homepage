---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "example-1", height, title } = Astro.props;
---

<DadsExampleFrame
  component="card"
  example={example}
  height={height}
  title={title}
/>
