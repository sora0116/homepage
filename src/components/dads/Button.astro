---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "all-buttons-using-button", height, title } = Astro.props;
---

<DadsExampleFrame
  component="button"
  example={example}
  height={height}
  title={title}
/>
