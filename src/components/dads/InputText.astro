---
import DadsExampleFrame from "./DadsExampleFrame.astro";

interface Props {
  example?: string;
  height?: string;
  title?: string;
}

const { example = "playground", height, title } = Astro.props;
---

<DadsExampleFrame
  component="input-text"
  example={example}
  height={height}
  title={title}
/>
