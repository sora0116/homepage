---
import { formatDate } from "../lib/db";
import type { WorkRecord } from "../lib/default-content";

interface Props {
  work: WorkRecord;
}

const { work } = Astro.props;
---

<article class="card">
  <p class="meta">{formatDate(work.publishedAt)}</p>
  <h3><a href={`/works/${work.slug}/`}>{work.title}</a></h3>
  <p>{work.summary}</p>
  <ul class="tag-list" aria-label="タグ">
    {work.tags.map((tag) => <li>{tag}</li>)}
  </ul>
  <div class="card-links">
    {work.links.map((link) => <a href={link.href}>{link.label}</a>)}
  </div>
</article>
